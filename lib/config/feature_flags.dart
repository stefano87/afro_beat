/// Store tab + Google Play in-app purchases.
const bool kEnablePremiumStore = true;

/// Beat pack products in the Store (separate Play Console products).
const bool kEnableBeatPacksInStore = false;
